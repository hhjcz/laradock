#
# copy to ../docker-composer.specific.yml
# hhj@centrum.cz
#
version: '2'

networks:
  default:
    ipam:
      config:
        - subnet: 172.19.0.0/16

services:
    workspace:
        build:
            context: ./workspace
            args:
                - INSTALL_SOAP=false
                - INSTALL_SNMP=false
